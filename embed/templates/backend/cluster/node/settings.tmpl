{[template "backend/common/header.tmpl" .]}

<div class="layui-fluid">
    <div class="layui-col-md12" style="padding: 5px;">
        <div class="layui-card">
            <div class="layui-card-body">

                <div class="layui-tab layui-tab-brief">
                    <ul class="layui-tab-title" id="status">
                        {[range $menu := .submenu]}
                            <li data-status="{[$menu.Number]}" {[if eq (print "/" $.admin_path "/" $menu.Link) $.CurrentPath]}class="layui-this"{[end]}><a href="/{[$.admin_path]}/{[$menu.Link]}?cluster_id={[$.cluster_id]}&node_id={[$.node_id]}">{[$menu.Name]}</a></li>
                        {[end]}
                    </ul>
                </div>

                <div class="layui-row" style="margin-top: 10px;font-size: 12px;">
                    
                    <div class="layui-col-md2 menu_list_left">
                        <div class="bg" style="margin-top: 3px;">
                            {[range $smenu := .setting_menu]}
                                {[if eq $smenu.Type "line"]}
                                <div class="line"></div>
                                {[else]}
                                <a href="/{[$.admin_path]}/{[$smenu.Link]}?cluster_id={[$.cluster_id]}&node_id={[$.node_id]}" {[if eq (print "/" $.admin_path "/" $smenu.Link) $.CurrentPath]}class="select"{[end]}>{[$smenu.Name]}</a>
                                {[end]}
                            {[end]}
                        </div>
                    </div>

                    <div class="layui-col-md10">
                        <div style="background: #fff; border-radius: 8px;margin-left: 8px;">
                        <form class="layui-form layui-form-pane" onclick="return false;">
                            <table class="layui-table">
                                <colgroup><col width="150"><col></colgroup>
                                <tbody>
                                    <tr>
                                        <td>节点名称 *</td>
                                        <td>
                                            <div class="layui-input-inline">
                                                <input type="text" name="full_name" autocomplete="off" class="layui-input" value="{[if .Data]}{[.Data.FullName]}{[end]}" placeholder="">
                                            </div>
                                        </td>
                                    </tr>

                                    <tr>
                                        <td>所属集群</td>
                                        <td>
                                            <div class="layui-input-inline">
                                                <input type="text" name="username" autocomplete="off" class="layui-input" value="{[if .Data]}{[.Data.Username]}{[end]}" placeholder="">
                                            </div>
                                            <p class="layui-font-12 pt5">比如22。</p>
                                        </td>
                                    </tr>

                                    <tr>
                                        <td>IP地址 *</td>
                                        <td>
                                            <div class="layui-input-inline">
                                                <input type="text" name="username" autocomplete="off" class="layui-input" value="{[if .Data]}{[.Data.Username]}{[end]}" placeholder="">
                                            </div>
                                            <p class="layui-font-12 pt5">比如22。</p>
                                        </td>
                                    </tr>

                                    <tr>
                                        <td colspan="2" >
                                            <input type="hidden" name="id" value="{[if .Data]}{[.Data.ID]}{[end]}">
                                            <button class="layui-btn" lay-filter="submit_admin" lay-submit>保存</button>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </form>
                        </div>
                    </div>
                </div>
                <!-- end -->
            </div>
        </div>
    </div>
</div>

<script type="text/javascript">

layui.config({base: '/static/layuiadmin/'}).use(['table','jquery'], function(){
///
var admin = layui.admin
,$ = layui.$
,form = layui.form
,table = layui.table;


///
});
</script>


<script type="text/html" id="cmdTpl">
<a class="layui-btn layui-btn-xs" lay-event='edit' onclick="Admin.open('【{{d.username}}】'+'编辑','edit?id={{d.id}}',500,400)">详情</a>
<a class="layui-btn layui-btn-danger layui-btn-xs" lay-event='delete' onclick="Admin.del(this, 'delete','{{d.id}}')">设置</a>
</script>

{[template "backend/common/footer.tmpl" .]}
